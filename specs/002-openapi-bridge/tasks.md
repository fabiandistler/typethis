# Tasks: 002-openapi-bridge

Status: implemented in PR.

## Code

- [x] `R/openapi.R`: public exports `to_openapi()`,
  `from_openapi()`, `write_openapi()`, `read_openapi()` with S3
  methods and internal helpers (`build_openapi`,
  `merge_function_into_openapi`, `rewrite_refs_to_components`,
  `openapi_schema_to_fields`, `openapi_property_to_field`,
  `openapi_to_validator`, `builtin_from_openapi_type`,
  `ensure_jsonlite`).
- [x] No changes to `R/json_schema.R`, `R/typed_function.R`, or
  `R/datacontract.R` — the bridge sits on top.

## Tests

- [x] `tests/testthat/test-openapi.R`: 16 test_that blocks covering:
  - Document shape (`openapi`, `info` defaults, `components.schemas`).
  - Nested model `$ref` rewriting.
  - Composite specs (`t_list_of("Tag")`, `t_union(...)`).
  - List input dedupes class names.
  - Typed function → POST path + JSON body + required derivation
    from formals.
  - Optional argument falls out of `required`.
  - Function whose return type is a model emits `$ref` response.
  - YAML and JSON file IO via `write_openapi` / `read_openapi`.
  - Import: enum + numeric range, $ref between schemas, inline
    nested object as its own model.
  - Round-trip YAML through write → read → from.
  - Error paths: unsupported input, missing `components.schemas`.

## Documentation

- [x] Vignette `vignettes/typethis-guide.Rmd`: new section
  "OpenAPI 3.1 Export (v0.5+)" before "Real-World Examples".
- [x] `README.md` features list: OpenAPI bullet added.
- [x] `ROADMAP.md`: v0.5 section added at the top, v0.4 demoted from
  "current".
- [x] `NEWS.md`: v0.5.0 entry.
- [x] `man/*.Rd`: regenerated by `roxygen2::roxygenise()`
  (to_openapi, from_openapi, write_openapi, read_openapi).

## Versioning

- [x] `DESCRIPTION` Version bumped to `0.5.0`.
