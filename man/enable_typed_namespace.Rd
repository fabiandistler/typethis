% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/namespace_retrofit.R
\name{enable_typed_namespace}
\alias{enable_typed_namespace}
\title{Enable type checking for an installed package, without editing it}
\usage{
enable_typed_namespace(
  pkgname,
  .specs = list(),
  .infer = TRUE,
  .validate = TRUE,
  .coerce = FALSE,
  .filter = NULL
)
}
\arguments{
\item{pkgname}{Single string. Name of an installed package.}

\item{.specs, .infer, .validate, .coerce, .filter}{Forwarded to
\code{\link[=enable_for_package]{enable_for_package()}} every time the hook fires.}
}
\value{
Invisibly, \code{pkgname}.
}
\description{
Registers a \code{setHook(packageEvent(pkgname, "onLoad"), ...)} handler
that runs \code{\link[=enable_for_package]{enable_for_package()}} over \code{pkgname}'s namespace each
time it loads, and applies the retrofit immediately if the package
is already loaded. Use this when you cannot — or do not want to —
add a \code{R/zzz.R} to the target package.

Because the hook fires \emph{after} R locks the namespace bindings,
\code{enable_typed_namespace()} retrofits each binding via the
unlock-modify-relock dance (see \code{as_typed_env(.unlock = TRUE)}).
Re-locking is guaranteed by \code{on.exit} even if a wrap step errors.

Typical use is from \code{.Rprofile} or an interactive session:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{typethis::enable_typed_namespace("dplyr")
library(dplyr) # functions are now wrapped on load
}\if{html}{\out{</div>}}

Standard package hook functions (\code{.onLoad}, \code{.onAttach}, ...) and
primitives are skipped automatically by \code{\link[=enable_for_package]{enable_for_package()}}.

This pattern is \strong{not for CRAN-bound code}: modifying another
package's namespace from outside is a developer convenience, not a
shipping feature. Use \code{\link[=enable_for_package]{enable_for_package()}} from your own
package's \code{.onLoad} for production code.
}
\examples{
\dontrun{
# In .Rprofile or an interactive session:
typethis::enable_typed_namespace("dplyr")
library(dplyr)

# Later, undo:
typethis::disable_typed_namespace("dplyr")
}
}
\seealso{
\code{\link[=disable_typed_namespace]{disable_typed_namespace()}} to remove the hook and revert
the retrofit; \code{\link[=enable_for_package]{enable_for_package()}} for the inside-the-package
variant that is suitable for CRAN.

Other typed functions:
\code{\link[=as_typed]{as_typed()}},
\code{\link[=as_typed_env]{as_typed_env()}},
\code{\link[=as_typed_from_roxygen]{as_typed_from_roxygen()}},
\code{\link[=default_type_vocabulary]{default_type_vocabulary()}},
\code{\link[=disable_typed_namespace]{disable_typed_namespace()}},
\code{\link[=enable_for_package]{enable_for_package()}},
\code{\link[=get_signature]{get_signature()}},
\code{\link[=infer_specs]{infer_specs()}},
\code{\link[=is_typed]{is_typed()}},
\code{\link[=parse_param_type]{parse_param_type()}},
\code{\link[=signature]{signature()}},
\code{\link[=typed_function]{typed_function()}},
\code{\link[=typed_method]{typed_method()}},
\code{\link[=types]{types()}},
\code{\link[=validate_call]{validate_call()}},
\code{\link[=with_signature]{with_signature()}}
}
\concept{typed functions}
