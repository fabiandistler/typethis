% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_typed.R
\name{types}
\alias{types}
\alias{types<-}
\title{Get or set the type specs of a function}
\usage{
types(f)

types(f) <- value
}
\arguments{
\item{f}{A function.}

\item{value}{A list of specs (or \code{NULL} to un-type).}
}
\value{
\code{types(f)} returns a list of specs. \code{types(f) <- value}
returns the modified function (assigned back to \code{f} by R).
}
\description{
Symmetric replacement-form accessor over \code{\link[=as_typed]{as_typed()}}. The setter
delegates to \code{as_typed()}; the getter returns specs in the shape
the setter accepts, so \code{types(f) <- types(g)} round-trips.
\itemize{
\item \code{types(f)} returns a list whose named entries are argument specs
and whose \code{.return} entry (if present) is the return spec.
Returns \code{list()} for an untyped function, so it is cheap to use
in \code{if (length(types(f))) ...} checks.
\item \code{types(f) <- value} retrofits \code{f} via \code{as_typed()}. \code{value} must
be a list — named arg specs plus any of \code{.return}, \code{.infer},
\code{.validate}, \code{.coerce}.
\item \code{types(f) <- NULL} un-types \code{f} (returns the original inner
function) — the natural inverse.
}
}
\examples{
add <- function(x = 0L, y = 0L) x + y
types(add) <- list(x = "integer", y = "integer", .return = "integer")
is_typed(add)
types(add)

# NULL un-types
types(add) <- NULL
is_typed(add)
}
\seealso{
\code{\link[=as_typed]{as_typed()}} for the underlying retrofit;
\code{\link[=as_typed_env]{as_typed_env()}} for bulk retrofits.

Other typed functions: 
\code{\link{as_typed}()},
\code{\link{as_typed_env}()},
\code{\link{as_typed_from_roxygen}()},
\code{\link{default_type_vocabulary}()},
\code{\link{disable_typed_namespace}()},
\code{\link{enable_for_package}()},
\code{\link{enable_typed_namespace}()},
\code{\link{get_signature}()},
\code{\link{infer_specs}()},
\code{\link{is_typed}()},
\code{\link{parse_param_type}()},
\code{\link{signature}},
\code{\link{typed_function}()},
\code{\link{typed_method}()},
\code{\link{validate_call}()},
\code{\link{with_signature}()}
}
\concept{typed functions}
