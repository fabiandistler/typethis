% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_typed.R
\name{enable_for_package}
\alias{enable_for_package}
\title{Enable type checking for an entire package}
\usage{
enable_for_package(
  pkgname,
  .specs = list(),
  .infer = TRUE,
  .validate = TRUE,
  .coerce = FALSE,
  .filter = NULL
)
}
\arguments{
\item{pkgname}{The package name (string), or directly an environment
to retrofit. Inside \code{.onLoad()} use the \code{pkgname} argument R passes
to your hook. Passing an environment is mainly useful for tests.}

\item{.specs, .infer, .validate, .coerce}{Forwarded to \code{\link[=as_typed_env]{as_typed_env()}}.
Per-function entries in \code{.specs} win over inference.}

\item{.filter}{Optional \verb{function(name, fn)} returning a single
logical. Applied \emph{after} the built-in skip list, so this can only
reduce what is retrofitted.}
}
\value{
Invisibly, the character vector of names that were
retrofitted.
}
\description{
Convenience wrapper for switching on typethis across every function in a
package namespace, with a single call from the package's \code{.onLoad()} hook.
Inside \code{.onLoad()} the namespace bindings are not yet locked, so each
function can be replaced in place with its typed wrapper.

Add \code{R/zzz.R} to your package containing:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{.onLoad <- function(libname, pkgname) \{
  typethis::enable_for_package(pkgname)
\}
}\if{html}{\out{</div>}}

That single line walks the namespace, infers type specs from each
function's literal atomic defaults (see \code{\link[=infer_specs]{infer_specs()}}), and replaces
every binding with a typed wrapper. Functions whose defaults cannot be
inferred are left untyped unless you add an explicit override via
\code{.specs}.

Standard package hook functions (\code{.onLoad}, \code{.onAttach}, \code{.onUnload},
\code{.onDetach}, \code{.Last.lib}, \code{.First.lib}) and primitives are skipped
automatically. A user-supplied \code{.filter} runs \emph{after} the default skip
list — it can narrow the set of retrofitted functions, not widen it.

Calling \code{enable_for_package()} from outside \code{.onLoad()} is safe but
usually redundant: the namespace is locked by then and most bindings
will be skipped with a single warning reporting the count.
}
\examples{
# Inside R/zzz.R of your own package:
# .onLoad <- function(libname, pkgname) {
#   typethis::enable_for_package(pkgname)
# }

# Demonstration on an ordinary environment shaped like a namespace:
ns <- new.env()
ns$add <- function(x = 0L, y = 0L) x + y
ns$.onLoad <- function(libname, pkgname) NULL
enable_for_package(ns)
is_typed(ns$add)        # TRUE  -- inferred from defaults
is_typed(ns$.onLoad)    # FALSE -- hook skipped

# Override one function while the rest are inferred
ns2 <- new.env()
ns2$add <- function(x = 0L, y = 0L) x + y
ns2$greet <- function(name = "world") paste0("hi ", name)
enable_for_package(ns2, .specs = list(
  add = list(.return = "integer")
))
}
\seealso{
\code{\link[=as_typed_env]{as_typed_env()}} for the underlying engine; \code{\link[=as_typed]{as_typed()}}
for per-function retrofits; \code{\link[=infer_specs]{infer_specs()}} for inference rules.

Other typed functions: 
\code{\link{as_typed}()},
\code{\link{as_typed_env}()},
\code{\link{as_typed_from_roxygen}()},
\code{\link{default_type_vocabulary}()},
\code{\link{get_signature}()},
\code{\link{infer_specs}()},
\code{\link{is_typed}()},
\code{\link{parse_param_type}()},
\code{\link{signature}},
\code{\link{typed_function}()},
\code{\link{typed_method}()},
\code{\link{types}()},
\code{\link{validate_call}()},
\code{\link{with_signature}()}
}
\concept{typed functions}
