% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roxygen_retrofit.R
\name{as_typed_from_roxygen}
\alias{as_typed_from_roxygen}
\title{Retrofit a package using its roxygen / Rd documentation}
\usage{
as_typed_from_roxygen(
  pkgname,
  .specs = list(),
  .infer = TRUE,
  .validate = TRUE,
  .coerce = FALSE,
  .filter = NULL,
  .vocabulary = default_type_vocabulary(),
  .rd_dir = NULL
)
}
\arguments{
\item{pkgname}{Package name (string) for an installed package, or
an environment together with \code{.rd_dir} for testing / source
workflows.}

\item{.specs, .infer, .validate, .coerce, .filter}{Forwarded to
\code{\link[=enable_for_package]{enable_for_package()}}. Per-function entries in \code{.specs} win
over Rd-derived specs.}

\item{.vocabulary}{A named character vector mapping perl-regex
patterns to spec strings. The first matching pattern wins, so
order matters. Defaults to \code{\link[=default_type_vocabulary]{default_type_vocabulary()}}.}

\item{.rd_dir}{Optional path to a directory containing \code{.Rd}
files. When supplied, files are parsed from disk instead of
via \code{tools::Rd_db()}. Useful for testing and for source
packages that have not been installed.}
}
\value{
Invisibly, the character vector of names that were
retrofitted.
}
\description{
Reads installed \code{.Rd} documentation for \code{pkgname}, extracts a type
spec for every documented \verb{\\\\arguments} item and \verb{\\\\value} block,
and applies the result via \code{\link[=enable_for_package]{enable_for_package()}}.

Two extraction layers run in sequence:
\itemize{
\item \strong{Explicit tag.} A description that begins with \verb{[type]}
(e.g. \verb{@param x [integer] Number of iterations}) is parsed
verbatim. Whatever sits between the brackets becomes the spec
string.
\item \strong{Vocabulary heuristic.} Otherwise the description's leading
prose is matched against a small vocabulary of common R type
names (\code{numeric}, \code{integer}, \code{character}, \code{logical}, ...). The
patterns are anchored to the start of the description, so prose
like "A numeric vector" matches but "the function applied to..."
does not. See \code{\link[=default_type_vocabulary]{default_type_vocabulary()}}.
}

The resulting \code{.specs} list is merged with anything you pass in
explicitly (your overrides win), filtered to formals that actually
exist on each function, then forwarded to \code{\link[=enable_for_package]{enable_for_package()}}.
That means inference from defaults still runs for arguments the
docs do not describe.

Use this when an existing package already has decent prose docs
but no type information — typethis lifts what is already written.
For specs the heuristics cannot recover, add explicit \verb{[type]}
tags in your roxygen, or pass them via \code{.specs}.
}
\examples{
# Inside R/zzz.R of your package:
# .onLoad <- function(libname, pkgname) {
#   typethis::as_typed_from_roxygen(pkgname)
# }

# Demonstration with a synthetic Rd directory:
rd_dir <- tempfile("rd")
dir.create(rd_dir)
writeLines(c(
  "\\\\name{add}",
  "\\\\alias{add}",
  "\\\\title{Add two numbers}",
  "\\\\arguments{",
  "\\\\item{x}{A numeric vector.}",
  "\\\\item{y}{[integer] Number of times.}",
  "}",
  "\\\\value{A numeric vector.}"
), file.path(rd_dir, "add.Rd"))

ns <- new.env()
ns$add <- function(x, y) rep(x, y)
as_typed_from_roxygen(ns, .rd_dir = rd_dir)
get_signature(ns$add)
}
\seealso{
\code{\link[=enable_for_package]{enable_for_package()}} for the underlying retrofit;
\code{\link[=parse_param_type]{parse_param_type()}} for the per-string extractor;
\code{\link[=default_type_vocabulary]{default_type_vocabulary()}} for the prose-to-spec mapping.

Other typed functions: 
\code{\link{as_typed}()},
\code{\link{as_typed_env}()},
\code{\link{default_type_vocabulary}()},
\code{\link{disable_typed_namespace}()},
\code{\link{enable_for_package}()},
\code{\link{enable_typed_namespace}()},
\code{\link{get_signature}()},
\code{\link{infer_specs}()},
\code{\link{is_typed}()},
\code{\link{parse_param_type}()},
\code{\link{signature}},
\code{\link{typed_function}()},
\code{\link{typed_method}()},
\code{\link{types}()},
\code{\link{validate_call}()},
\code{\link{with_signature}()}
}
\concept{typed functions}
