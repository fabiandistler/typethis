% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_typed.R
\name{as_typed_env}
\alias{as_typed_env}
\title{Bulk-retrofit every function in an environment}
\usage{
as_typed_env(
  envir,
  .specs = list(),
  .infer = TRUE,
  .validate = TRUE,
  .coerce = FALSE,
  .filter = NULL,
  .unlock = FALSE
)
}
\arguments{
\item{envir}{An environment. Use \code{\link[=new.env]{new.env()}} or \code{\link[=globalenv]{globalenv()}} for
ordinary cases; passing a namespace is supported but most
bindings will be locked.}

\item{.specs}{Named list of per-function override lists. Each entry
has the same shape as \code{as_typed()}'s \code{...} plus the dotted
options. Names must correspond to functions in \code{envir}; unknown
names are an error.}

\item{.infer, .validate, .coerce}{Function-level defaults forwarded
to \code{\link[=as_typed]{as_typed()}}. Per-function entries in \code{.specs} win.}

\item{.filter}{Optional \verb{function(name, fn)} returning a single
logical; functions for which this returns \code{FALSE} are skipped.}

\item{.unlock}{If \code{TRUE}, locked bindings are temporarily unlocked,
reassigned to their typed wrapper, and re-locked. Defaults to
\code{FALSE}, in which case locked bindings are skipped with a
warning. Re-locking is guaranteed by \code{on.exit} even if a wrap
step errors.}
}
\value{
Invisibly, the character vector of names that were
retrofitted.
}
\description{
Walks \code{envir}, applies \code{\link[=as_typed]{as_typed()}} to every function it finds, and
reassigns the result back into \code{envir} in place. Useful for adding
type checks across a script, a chunk of analysis code in
\code{\link[=globalenv]{globalenv()}}, or a private environment held by a package.

Per-function overrides flow through \code{.specs}, a named list of lists
whose entries match the \code{as_typed()} argument shape (named arg specs
plus optional \code{.return}/\code{.infer}/\code{.validate}/\code{.coerce}). Functions
without a \code{.specs} entry are still retrofitted via inference (when
\code{.infer = TRUE}) and the function-level defaults.

Already-typed functions are re-wrapped through \code{as_typed()}'s
idempotent merge path — no double-wrapping. Locked bindings (common
for namespaces) are skipped by default; a single warning reports the
count. Pass \code{.unlock = TRUE} to unlock-modify-relock each binding in
place — this is what \code{\link[=enable_typed_namespace]{enable_typed_namespace()}} uses to retrofit
bindings \emph{after} a namespace has been locked.
}
\examples{
e <- new.env()
e$add <- function(x = 0L, y = 0L) x + y
e$greet <- function(name = "world") paste0("hi ", name)
as_typed_env(e)
is_typed(e$add)
is_typed(e$greet)

# Per-function overrides
e2 <- new.env()
e2$add <- function(x = 0L, y = 0L) x + y
as_typed_env(e2, .specs = list(
  add = list(x = "integer", y = "integer", .return = "integer")
))

# Filter to a subset
e3 <- new.env()
e3$keep <- function(x = 1L) x
e3$skip <- function(x = 1L) x
as_typed_env(e3, .filter = function(name, fn) name == "keep")

# Unlock-and-modify locked bindings (e.g. after a namespace lock)
e4 <- new.env()
e4$add <- function(x = 0L) x
lockBinding("add", e4)
as_typed_env(e4, .unlock = TRUE)
is_typed(e4$add)
}
\seealso{
\code{\link[=as_typed]{as_typed()}} for the per-function form; \code{\link[=types]{types()}} for
the replacement-form accessor; \code{\link[=enable_typed_namespace]{enable_typed_namespace()}} for
the \code{setHook}-based whole-package wrapper.

Other typed functions:
\code{\link[=as_typed]{as_typed()}},
\code{\link[=as_typed_from_roxygen]{as_typed_from_roxygen()}},
\code{\link[=default_type_vocabulary]{default_type_vocabulary()}},
\code{\link[=disable_typed_namespace]{disable_typed_namespace()}},
\code{\link[=enable_for_package]{enable_for_package()}},
\code{\link[=enable_typed_namespace]{enable_typed_namespace()}},
\code{\link[=get_signature]{get_signature()}},
\code{\link[=infer_specs]{infer_specs()}},
\code{\link[=is_typed]{is_typed()}},
\code{\link[=parse_param_type]{parse_param_type()}},
\code{\link[=signature]{signature()}},
\code{\link[=typed_function]{typed_function()}},
\code{\link[=typed_method]{typed_method()}},
\code{\link[=types]{types()}},
\code{\link[=validate_call]{validate_call()}},
\code{\link[=with_signature]{with_signature()}}
}
\concept{typed functions}
