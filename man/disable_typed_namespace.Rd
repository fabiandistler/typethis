% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/namespace_retrofit.R
\name{disable_typed_namespace}
\alias{disable_typed_namespace}
\title{Remove a typethis hook and revert the typed wrappers}
\usage{
disable_typed_namespace(pkgname, .revert = TRUE)
}
\arguments{
\item{pkgname}{Single string. Name of an installed package.}

\item{.revert}{If \code{TRUE} (default), each currently typed binding in
\code{pkgname}'s namespace is reverted to its inner function via the
same unlock-modify-relock dance. Pass \code{FALSE} to leave the loaded
namespace as-is and only stop the retrofit on future loads.}
}
\value{
Invisibly, the character vector of names that were
reverted (empty when \code{.revert} is \code{FALSE} or the package is not
loaded).
}
\description{
The inverse of \code{\link[=enable_typed_namespace]{enable_typed_namespace()}}. Removes any typethis
hook previously registered for \code{pkgname} so future loads of the
package are not wrapped, and (by default) walks the currently
loaded namespace to replace each typed wrapper with the original
inner function.

Hooks added by other code (anything \emph{not} tagged by typethis) are
left intact, so this is safe to call when third-party hooks coexist
on the same package event.
}
\examples{
\dontrun{
typethis::enable_typed_namespace("dplyr")
library(dplyr)
typethis::disable_typed_namespace("dplyr")
}
}
\seealso{
\code{\link[=enable_typed_namespace]{enable_typed_namespace()}} for the registration side.

Other typed functions:
\code{\link[=as_typed]{as_typed()}},
\code{\link[=as_typed_env]{as_typed_env()}},
\code{\link[=as_typed_from_roxygen]{as_typed_from_roxygen()}},
\code{\link[=default_type_vocabulary]{default_type_vocabulary()}},
\code{\link[=enable_for_package]{enable_for_package()}},
\code{\link[=enable_typed_namespace]{enable_typed_namespace()}},
\code{\link[=get_signature]{get_signature()}},
\code{\link[=infer_specs]{infer_specs()}},
\code{\link[=is_typed]{is_typed()}},
\code{\link[=parse_param_type]{parse_param_type()}},
\code{\link[=signature]{signature()}},
\code{\link[=typed_function]{typed_function()}},
\code{\link[=typed_method]{typed_method()}},
\code{\link[=types]{types()}},
\code{\link[=validate_call]{validate_call()}},
\code{\link[=with_signature]{with_signature()}}
}
\concept{typed functions}
