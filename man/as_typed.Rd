% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_typed.R
\name{as_typed}
\alias{as_typed}
\title{Retrofit type stability onto an existing function}
\usage{
as_typed(
  fn,
  ...,
  .return = NULL,
  .infer = TRUE,
  .validate = TRUE,
  .coerce = FALSE
)
}
\arguments{
\item{fn}{The function to retrofit.}

\item{...}{Named type specs. Names must match formals of \code{fn}. Values may
be character builtins, predicates, or \link{type_spec} objects.
Pass \code{NULL} to opt a single argument out of inference.}

\item{.return}{Specification for the return value, or \code{NULL} to skip.
The return type is never inferred.}

\item{.infer}{If \code{TRUE} (default), arguments not named in \code{...} get a spec
inferred from their default value when possible. See \code{\link[=infer_specs]{infer_specs()}}.}

\item{.validate}{If \code{FALSE}, type checks are skipped (useful for hot paths).}

\item{.coerce}{If \code{TRUE}, arguments that don't match are first run through
\code{\link[=coerce_type]{coerce_type()}} before assertion.}
}
\value{
A typed function. Same shape as the result of \code{\link[=typed_function]{typed_function()}}.
}
\description{
Convenience wrapper around \code{\link[=typed_function]{typed_function()}} for adding type checks to a
function you already have. Compared to \code{\link[=typed_function]{typed_function()}} it removes two
sources of friction:
\itemize{
\item Argument specs are passed via \code{...} instead of \code{arg_specs = list(...)}.
\item Specs for arguments with literal atomic defaults are inferred
automatically (see \code{\link[=infer_specs]{infer_specs()}}); only the arguments you care about
need to appear in \code{...}.
}

Internally \code{as_typed()} builds an \code{arg_specs} list and delegates to
\code{\link[=typed_function]{typed_function()}}, so all calling conventions, coercion, and metadata
behaviour are identical.
}
\examples{
# Inferred from defaults — no spec list needed
add <- as_typed(function(x = 0L, y = 0L) x + y, .return = "integer")
add(2L, 3L)

# Override one argument; the rest are inferred
greet <- as_typed(
  function(name = "world", times = 1L) {
    paste(rep(name, times), collapse = " ")
  },
  name = t_vector_of("character", exact_length = 1L)
)
greet("hi", times = 3L)

# Opt an argument out of validation with NULL
f <- as_typed(function(x = 1L, y = 1L) x + y, y = NULL)
attr(f, "arg_specs")

# Disable inference entirely
g <- as_typed(function(x = 1L, y = 2L) x + y, .infer = FALSE)
attr(g, "arg_specs")
}
\seealso{
\code{\link[=infer_specs]{infer_specs()}} for the inference rules; \code{\link[=typed_function]{typed_function()}} for
the underlying wrapper.

Other typed functions: 
\code{\link{as_typed_env}()},
\code{\link{as_typed_from_roxygen}()},
\code{\link{default_type_vocabulary}()},
\code{\link{disable_typed_namespace}()},
\code{\link{enable_for_package}()},
\code{\link{enable_typed_namespace}()},
\code{\link{get_signature}()},
\code{\link{infer_specs}()},
\code{\link{is_typed}()},
\code{\link{parse_param_type}()},
\code{\link{signature}},
\code{\link{typed_function}()},
\code{\link{typed_method}()},
\code{\link{types}()},
\code{\link{validate_call}()},
\code{\link{with_signature}()}
}
\concept{typed functions}
